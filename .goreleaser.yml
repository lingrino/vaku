builds:
  - binary: vaku
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
changelog:
  sort: asc
  filters:
    exclude:
    - Merge pull request
    - Merge branch
git:
  short_hash: true
